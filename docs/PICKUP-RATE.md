# 分层取件限流

本规则适用于 v0.2.0 之后的共享收件修复，替代共享模式的“每次完成后再冷却 2 秒”。取码响应的 JSON 结构保持不变；Apple 创建限额与本规则无关。

## 默认共享模式

- 同一个 root 的 Pool、Bearer、取件链接共用速率桶：容量 2，每 2 秒补 1，最多存 2 次。切换凭据或 `+tag` 不产生新桶。
- 开始一个合法取件请求扣 1 次；最多容纳 1 个普通请求及 1 个 Pool 长等待。已经有等待请求时，普通链接仍可读取；第二个长等待即时 429，不堆积队列。
- 请求执行、等待新码期间额度持续恢复。结束、失败、取消均不把恢复时间推到“现在再加 2 秒”。失败请求已消耗的额度不返还，避免错误重试打满上游。
- 拦截不扣额度、不重置时间、不占用新的 alias/主号名额。其他 root 使用各自额度。
- 上游保护独立：同主号共享同步、最多 1 个读取连接、实际启动最短 2 秒、全局 IMAP 并发有界、失败退避。允许一次快速刷新不表示重新登录 Apple。
- HTTP 总入口仍受每秒 100 次、128 个在途、每主号 64 个限制，包含等待请求；主号读取与 HTTP 等待人数是两套预算。

关闭共享模式的旧路径可能逐次直连上游，因此仍保持单 alias 独占、完成后 2 秒冷却、全站 16 / 主号 2 个在途。

## 典型时序

| 操作 | 结果 |
| --- | --- |
| 空闲后读取一次，再快速刷新一次 | 2 次都可准入，复用主号收件结果 |
| 同一时刻第三次请求 | 429，约 2 秒后恢复 1 次 |
| 1.8 秒时再次刷新 | 429，剩余约 200 ms；不重新开始 2 秒 |
| 2 秒时刷新 | 恢复 1 次准入 |
| 一个长等待期间普通读取 | 有速率额度时正常准入；不取消等待者 |
| 已等待 15 秒的请求结束后立刻再取 | 若无其他读者消耗，已有恢复额度，直接准入 |
| 同 root 再开第二个长等待 | 429；结束已有请求或使用普通读取 |

## 429 原因

JSON 错误码继续为 `RATE_LIMITED`，HTTP 状态继续为 429。附加响应头：

| `X-RateLimit-Reason` | 含义 |
| --- | --- |
| `mailbox_rate` | 同 root 突发额度已用完 |
| `mailbox_waiting` | 同 root 已有一个长等待 |
| `mailbox_inflight` | 同 root 已有一个普通读取 |
| `mailbox_cooldown` | 旧非共享路径完成后的冷却 |
| `account_concurrency` | 主号 HTTP 在途名额已满 |
| `global_concurrency` | 全局 HTTP 在途名额已满 |
| `global_rate` | 全局每秒入口额度已满 |
| `mailbox_capacity` | 有界 alias 状态表已满 |

`Retry-After` 是剩余等待时间向上取整后的秒数；`X-Retry-After-Ms` 为毫秒值。对在途、等待或容量繁忙，返回的是建议复查间隔，不是已知完成时刻。采样日志记录 `limit_reason`、`retry_after_ms`，不记录凭据或邮件。

客户端优先采用一个 `wait_seconds` 请求，按总 deadline 处理 `no_code`、429、503，不把空结果当永久失败；不要同时启动多个同 root 长等待。轮询可保持 2 秒间隔，但无需在已经等待很久的请求结束后再固定睡 2 秒。普通读取仍自行鉴权、复验租约与凭据，增加额度不放宽项目/邮箱隔离。

## 验证

离线测试先复现旧规则：长等待挡住普通读取、结束后额外冷却。新测试覆盖：跨凭据共享突发额度、连续拦截不推迟恢复、精确边界与 Retry-After、长等待取消/超时、双槽隔离、重复释放、主号容量不消耗其他 root 额度及全局窗口固定。真实 TLS IMAP fixture 继续覆盖 40 roots、错峰 `+tag` 新信、仅 2 次上游登录及 82 封目标正文各读一次。

```sh
flock .local/project-heavy.lock env GOMAXPROCS=2 GOMEMLIMIT=512MiB GIN_MODE=release go test -p 1 -parallel 2 ./internal/httpserver -run 'TestSharedPickup|TestPickup|TestActiveReceiverHTTP|TestPoolLeaseCodeWait' -count=1
```

### 公网只读取件验证（2026-09-20）

代码版本 `a23e516` 从干净提交构建；更新应用前确认创建、删除、Pool retirement 均无活跃作业，自动创建计划未启用。仅更新应用容器，数据库容器保持运行，保留旧应用镜像以供回滚。

使用已有取件链接，经公网代理实测，未发新信或执行 Apple 创建/删除：

| 请求 | HTTP 状态 | 耗时 | 剩余额度恢复时间 |
| --- | --- | --- | --- |
| 首次预热 | 200 | 1904 ms | - |
| 空闲恢复后第 1 次读取 | 200 | 27 ms | - |
| 紧接第 2 次读取 | 200 | 20 ms | - |
| 紧接第 3 次读取 | 429 | 13 ms | 1958 ms |
| 约 300 ms 后再刷新 | 429 | 16 ms | 1642 ms |
| 再约 300 ms 后刷新 | 429 | 15 ms | 1327 ms |
| 按剩余时间等待后重试 | 200 | 415 ms | - |

三次 429 原因均为 `mailbox_rate`，连续刷新没有推迟恢复。运行日志确认共享收件启用、周期拉取关闭，限流记录包含原因和剩余毫秒。上述 200 仅证明真实取件接口与限流恢复正常，不代表本轮验证了新邮件投递；新 `+tag` 邮件唤醒由离线 TLS IMAP fixture 覆盖。

同一干净代码版本的 5 个受影响 Go 包测试通过；工作树聚焦 race、`go vet`、发布文档测试和 `git diff --check` 通过。原有未提交的 Pool 接口改动未纳入镜像。
