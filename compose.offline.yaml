# Load the release image archive before starting with this overlay.
services:
  icloud-api:
    image: icloud-api:v0.2.2
    pull_policy: never
  postgres:
    image: icloud-api-postgres:v0.2.2
    pull_policy: never
